apply plugin: "org.sonarqube"

sonarqube {
    properties {
        property "sonar.projectKey", "Mono-FE"
        property "sonar.organization", "test-mono-repo"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}
